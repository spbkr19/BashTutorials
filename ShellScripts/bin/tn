#!/bin/bash

# This is a simple note taking bash script
# Author: Karthik

# echo $1 >> ~/notes.txt
#echo $* >> ~/notes.txt

# I want to add date now
# In order to call the script from anywhere in the machine
# PATH=$PATH:<PathNameFromRoot>

echo $(date -u): $* >> ~/notes.txt
